{
  "name": "openyila",
  "private": true,
  "version": "0.1.5",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "node scripts/link-web-node-modules.mjs",
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace packages/core run build && npm --workspace apps/web run build",
    "build:app": "npm --workspace packages/core run build && npm --workspace apps/web run build:app",
    "build:mp-weixin": "npm --workspace packages/core run build && npm --workspace apps/web run build:mp-weixin",
    "test": "npm --workspace packages/core run test"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.19"
  }
}
